# KANT 직원용 툴 모음

칸트 직원들이 브라우저로 쓰는 정적 도구 사이트다.
GitHub Pages 로 배포된다: https://jjiya1225-tech.github.io/KANT_SMART_TOOLS/

## 먼저 지킬 것 — 여러 세션이 이 저장소를 만진다

1. **작업 전에 반드시 `git pull`.** 다른 클로드 세션이 방금 푸시했을 수 있다.
2. **한 툴을 고치면 바로 커밋·푸시한다.** 로컬에 묵히면 다른 세션과 충돌한다.
3. 푸시 후 Pages 배포는 ~1분. 배포 확인은 라이브 URL 에서 바뀐 문구를 grep 한다.

## 구조

```
index.html      허브 (카테고리별 카드)
tools.js        툴 목록 매니페스트 — 새 툴은 여기 등록해야 허브에 뜬다
tools/<이름>/index.html   툴 하나 = 자기완결 단일 HTML (외부 서버 없음)
```

새 툴 규칙: 단일 index.html, 의존성 없이 동작(CDN 스크립트 정도만),
드래그앤드롭·클립보드 우선, 한국어 UI. `tools.js` 에 카드 등록.

## 툴들이 이루는 흐름 (발주 → 입고 → 판매 → 출고)

```
발주 시스템(order-system)      수량 입력 → 공장별 발주서 엑셀 (품목명 = 한국품명)
        ↓ 중국 공장
패킹리스트(箱单) 수신           韩文型号(한국품명) 열이 되돌아온다 — 이 열이 매칭의 열쇠
        ↓
인보이스 변환기(invoice-converter)
        단가 산정(자동 올림: >1,000원은 100원, 이하 10원 단위)
        '이카운트입고' 시트: 품목코드(빈칸)·한국품명·수량·매입단가
        무상 부속(단가 0)은 앞 품목의 '동봉 부속' 칸으로 접는다
        ↓ 클로드 코드("이카운트 재고에 넣어줘")
이카운트 구매입력 → 재고 반영
        ↓ 판매 후
로젠 변환기(logen-converter)   배송처리/판매조회 엑셀 ↔ 로젠 송장 (수기 접수분은 이름·전화 매칭)
```

이카운트 쪽 절차·규칙은 **사무실 PC 의 ecount 작업 폴더 CLAUDE.md** 가 원본이다.
(재고 반영·품목코드 매칭·ERP전송 함정 등은 거기에 적혀 있다. 여기엔 중복해 적지 않는다.)

## 하고 싶은 것 (예정)

- **발주 카탈로그 ↔ 이카운트 품목코드 공유 매핑**: order-system 의 CATALOG 품목명마다
  이카운트 코드를 달아 공유 파일(`tools/shared/`)로 두고, 인보이스 변환기가 그걸 읽어
  '이카운트입고' 시트의 품목코드를 자동으로 채운다.
  ⚠ 자동 매칭만으로 만들면 안 된다 — 계열 오인 사례 있음
  (예: "5mm 마그네틱 레일"이 M28 레일로 붙는다). 사람 검토를 거쳐 확정한다.
- order-system 은 병행 세션에서 수정 중이니, 카탈로그 구조를 바꾸기 전에 pull 로 확인.

## 커밋 규칙

한국어로, 무엇이 왜 바뀌었는지 한 줄. 끝에:
Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
